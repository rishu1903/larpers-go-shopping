numpy>=1.26,<3
scikit-learn>=1.5,<2
joblib>=1.4,<2

# Optional: local semantic fallback for override
# detection (src/override_semantic.py). The agent
# still functions correctly without it (falls back to
# regex-only override detection) -- see the "Override
# Detection" section of README.md. Install CPU-only
# torch first to avoid pulling large CUDA wheels:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install sentence-transformers
sentence-transformers>=3.0,<4

# Optional: LLM-driven component-scoped item-context
# tracking (src/llm_client.py, src/state_tracker.py).
# OFF by default -- only activates if ANTHROPIC_API_KEY
# is set. With no key, SessionState.update() uses only
# the deterministic path above; this dependency is not
# imported at all. See "V14 -- Component-Scoped Item
# Context" in README.md.
# anthropic>=0.40,<1
